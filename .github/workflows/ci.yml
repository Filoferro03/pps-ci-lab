on:
  push:
jobs:
  Build:
    runs-on: ubuntu-latest
    env:
      TERM: dumb
    steps:
      - uses: actions/setup-java@v5
        with:
          java-version: 25
      - name: Build
        run: ./gradlew check